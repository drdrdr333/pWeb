runtime: python
env: flex
entrypoint: gunicorn -b :$PORT main:app

runtime_config:
  python_version: 3


beta_settings:
  cloud_sql_instances: solid-space-326918:us-east1:baseball-pitchers-127.0.0.1=tcp:3306



manual_scaling:
  instances: 1
resources:
  cpu: 1
  memory_gb: 0.5
  disk_size_gb: 10
