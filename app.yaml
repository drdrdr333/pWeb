runtime: python
env: flex
entrypoint: gunicorn -b :$PORT main:app

runtime_config:
  python_version: 3
  
env_variables:
  SQLALCHEMY_DATABASE_URI = postgresql+psycopg2://postgres:123456@35.185.16.7:5433/baseball_pitchers?host=/cloudsql/solid-space-326918:us-east1:baseball-pitchers
    
beta_settings:
  cloud_sql_instances: solid-space-326918:us-east1:baseball-pitchers

manual_scaling:
  instances: 1
resources:
  cpu: 1
  memory_gb: 0.5
  disk_size_gb: 10
